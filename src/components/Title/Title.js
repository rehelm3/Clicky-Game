import React from "react";
import "./Title.css";

const Title = () =>
    <div className="titlebar">
        <h1 className="title">The Game to End All Games</h1>
        <h2 className="title2">Click on an image to earn points, but don't click on any more than once!</h2>
    </div>
    ;

export default Title;
